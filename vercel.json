{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "installCommand": "cd backend && npm ci && cd ../frontend && npm ci",
  "buildCommand": "cd backend && npm run build:vercel && cd ../frontend && npm run build"
}

